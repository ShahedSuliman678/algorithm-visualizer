const queue=[];
const visted=[];
queue.push(startCell);  // only the start goes in first
visited[startCell.r][startCell.c] = true;
while(queue>0){
    const current = queue.shift();
    if (current.r === endCell.r && current.c === endCell.c) {
        
    }
}